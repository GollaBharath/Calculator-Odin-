function btnmg(event) {
  const btn = event.target.textContent;
  if (!isNaN(parseInt(btn))) {
    console.log(btn);
  } else {
    console.log(btn);
  }
}

function firstnum(a) {
  return parseInt(a);
}
